version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: Run Hello World
          command: echo "Hello SnapForge! CI/CD pipeline is working ✅"
